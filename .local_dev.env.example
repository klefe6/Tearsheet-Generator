rem Local-only dev flags (optional). Copy to .local_dev.env on your machine.
rem Loaded by reboot_tkp_ts / reboot_tcp_ts / reboot_mp_ts — NOT production env files.
rem Enables password-free /admin/tearsheet on 127.0.0.1 only (see tearsheet_local_admin.py).
set "TEARSHEET_LOCAL_DIRECT_ADMIN=1"
