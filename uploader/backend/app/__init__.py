"""Glenn Daily Uploader — backend application package.

A small, self-contained FastAPI service that lets Glenn enter daily values for
the TKP / TCP / AGM / Y&Q programs. Backend-only. It has **no** dependency on and
does **not** import any of the existing tearsheet apps.
"""

__version__ = "0.1.0"
