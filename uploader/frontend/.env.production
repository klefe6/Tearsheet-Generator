# Production environment (used by `npm run build`).
# Placeholder API base — replace with the real uploader backend URL when it exists.
# The frontend does NOT call this endpoint yet; export is a mock action for now.
VITE_APP_ENV=production
VITE_API_BASE_URL=https://REPLACE-ME.example.com/api
